apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: mcp-fh-swf

resources:
  - deployment.yaml
  - ingress.yaml
  - pvc.yaml
  - neo4j.yaml

images:
  - name: fh-swf-mcp
    newName: ghcr.io/fhswf/mcp_fh-swf
    newTag: "0.0.0-placeholder"  # wird durch kustomize overlays überschrieben
